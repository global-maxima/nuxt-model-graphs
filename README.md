# nuxt-model-graphs
Reusable Nuxt module providing typed graph components and a unified renderer for visualizing model data using ECharts. Designed for consistent graphing across Nuxt apps with shared types, auto-registered components, and optional configuration.
